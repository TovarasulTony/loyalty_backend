[common]
common_length = 20
wallet_address_length = 55 #is 56 - 55, the G is not calculated; 55 random characters are generated and G si added in code,

[user]
min_user = ${common:common_length}
max_user = ${common:wallet_address_length}

[brand]
name_length = ${common:common_length}
program_length = 120

[shop]
location_length = ${common:common_length}
wallet_address_length = ${common:wallet_address_length}

[transation]
date_length = ${common:common_length}
