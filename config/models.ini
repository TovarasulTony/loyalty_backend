[common]
common_length = 20
wallet_address_length = 56

[user]
nickname_length = ${common:common_length}
wallet_address_length = ${common:wallet_address_length}
user_type_length = ${common:common_length}

[brand]
name_length = ${common:common_length}
program_length = 120

[shop]
location_length = ${common:common_length}
wallet_address_length = ${common:wallet_address_length}
